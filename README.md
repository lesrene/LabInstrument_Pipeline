# LabInstrument_Pipeline
For this project, I built an ETL data pipeline that deals with messy TRIOS-like JSONs containing simulated lab instrument data. After cleaning, the data is sent to a searchable dashboard for to track sample status, monitor data quality, and query results. 
